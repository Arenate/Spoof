# Spoof
My network project for reblox exploit which u can just do simple functions for example like having huge code that loops all remotes and table insert u could just do Spoof.FireAll(YourNumberAmount) just a single line easy.
(functions for now)
Spoof.SendBytes(10) (for all remotes) or specific
Spoof.Get(game.ReplicatedStorage.SomeRemote):Amount(100)
[this is all still in work these functions work for now but more functions coming]
